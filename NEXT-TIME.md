# Next time this template is used

- Show only the players card. No roll log, no final results section.
- The card updates live as players roll, and rows re-sort by assigned year (earliest on top). Players still waiting go below the rolled ones.
- Keep personal key links and the auto-assign of the last player ("Last player - auto-assigned").
- Save a server timestamp with each roll. This draw did not, so the roll order had to be entered by hand afterwards.
